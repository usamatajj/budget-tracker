'user client';

import { Container, LoginContainer } from './login.styles';

const LoginPage = () => {
  return (
    <Container>
      <LoginContainer>
        <h1>User Login</h1>
      </LoginContainer>
    </Container>
  );
};

export default LoginPage;
